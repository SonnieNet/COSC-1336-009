# Sonnie Chauvin
# Nov 3, 2025
# Classwork #55
#--------------------
# Project Objectives
#   Learning Loops ; will continue to ask prompt until 0 (sentinal) is entered (will end loop); at completetion, will print # of values entered AND sum of values entered
#--------------------

#This function will display the start of project
def projectStart():
    print("-" * 60)
    print("\tStart of Project")
    print("\tWritten By Sonnie Chauvin")
    print('\tLearning Loops')
    print("-" * 60)

#This function will display the end of project
def projectEnd():
    print("\n")
    print("-" * 60)
    print("\tEnd of Project")

# This function will get the user's entry for a float
def getFloatData(prompt):
   value = float(input(prompt))

   return value

# This function will get the user's entry for an integer
def getIntegerData(prompt):
    value = int(input(prompt))

    return value

#this function will get user's entry of string
def getStringData(prompt):
    value = input(prompt)

    return value

def main():
    #Display Start of Project
    projectStart()

    # enter integers until 0
    # display how many numbers entered
    # display the sum of numbers

    counter = 0 #sets the stopwatch
    sumNumbers = 0 #accumulator

# Pre-read before loop
    value = getIntegerData('\tEnter an integer. 0 will terminate: ')

    while(value != 0):

        counter = counter +1 # increment by 1
        sumNumbers = sumNumbers + value #adding the entered valid numbers
        
            # Updated loop
        value = getIntegerData('\tEnter an integer. 0 will terminate: ')

    print('\tNumber of value entered: ', counter)
    print('\tThe sums: ', sumNumbers)
    
        
    #Display end of project
    projectEnd() 
    
main()

#trace: 
